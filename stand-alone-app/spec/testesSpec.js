describe('Testes (specs)', function() {
  it('deve garantir que "true" é "true"', function() {
    expect(true).toBe(true);
  });

  it('deve garantir que "false" é "false"', function() {
    expect(false).toBe(false);
  });
});
